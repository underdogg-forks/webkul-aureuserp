#[\PHPUnit\Framework\Attributes\Test]
#[\PHPUnit\Framework\Attributes\Group('crud')]
/**
 * @test
 * @group crud
 * @covers \Modules\{{ module }}\Filament\{{ panel_namespace }}\Resources\{{ model }}Resource
 * @payload
 * {{ payload_json }}
 */
public function it_fails_to_create_{{ model_lower }}_when_required_fields_are_missing(): void
{
    $this->markTestIncomplete();

    //$this->actingAs(User::factory()->create());

    $payload = [
        {{ payload_array }}
    ];

    Livewire::test(Create{{ model }}::class)
        ->fillForm($payload)
        ->call('create')
        ->assertHasFormErrors();

    if (app()->isLocal()) {
        dump($payload);
    }
}
